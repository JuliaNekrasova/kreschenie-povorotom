source "https://rubygems.org"

# Локальная сборка сайта в точности как на GitHub Pages.
# На самом GitHub Pages этот Gemfile не используется — там своя сборка.
gem "github-pages", group: :jekyll_plugins

# Ускоряет пересборку при `jekyll serve --livereload` в локальной разработке.
gem "webrick"
